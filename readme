专注于IP地址服务领域的纯真（CZ88.NET），自2005年起便持续为广大社区用户提供社区版IP地址库服务。用户只需获得纯真官方授权，即可免费使用该版本IP库，同时还能持续获取后续的更新内容，满足日常IP地址查询的基础需求，有相关需求的用户可直接前往纯真官网提交申请。除了普惠性的社区版IP库外，纯真还推出了面向商业场景的IP地址查询数据服务，相较于社区版，商业版具备数据精度更高、配套服务更周全的显著优势。依托“网络空间拓扑测绘+移动位置大数据”的核心技术方案，纯真围绕IP地址构建了多维度的服务能力，经过近20年的深度数据沉淀与技术打磨，目前已覆盖IP地址定位、IP网络风险评估、IP使用场景识别、IP网络类型判定，以及秒拨侦测、VPN侦测、代理侦测、爬虫侦测、用户真人度识别等多个细分领域，能够为各类商业场景提供精准、可靠的IP数据支撑，助力企业提升网络服务与安全管理效率。

将czdb数据库导出为文本库，可以导出全部或按区域导出。运行需要安装php composer。
如果composer没有安装，可以通过下面的文件安装：
# 1. 下载 Composer 安装器
curl -sS https://getcomposer.org/installer | php

# 2. 移动 composer.phar 到全局可执行目录
sudo mv composer.phar /usr/local/bin/composer
或创建连接
sudo ln -s  /pathto/composer.phar  /usr/local/bin/composer
# 3. 验证安装
composer --version

完成安装后将
czdb_ip_exporter.php及composer.json拷贝到目录下，然后在目录下执行
composer install
即可运行脚本
